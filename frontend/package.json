{
  "name": "metro3d-frontend",
  "version": "1.0.0",
  "description": "Tokyo Metro 3D Operation Visualizer - Frontend",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "^0.165.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^5.2.0",
    "@types/three": "^0.165.0",
    "@types/node": "^20.12.0",
    "vitest": "^1.6.0"
  }
}
